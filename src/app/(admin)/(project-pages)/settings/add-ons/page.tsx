import PlaceholderPage from "@/components/common/PlaceholderPage";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Add-ons",
};

export default function Page() {
  return <PlaceholderPage title="Add-ons" />;
}
